#include <bits/stdc++.h>

using namespace std;

//another way to shorten code is to defien macross, macro vaneko certain strings in the code will be changed before the compilation. macro lai #define keyword le defined garna sakinxaaa,,,

//defining macrosss.
#define F first 
#define S second 
#define PB push_back 
#define MP make_pair

int main () {
  int a,b,c,d, e;
  //v.push_back(make_pair(a,b));
  //v.push_back(make_pair(c, d));
  //int e = v[i].first+v[i].second;
  //can be shortened too::
  // v.PB(MP(a,b));
  // v.PB(MP(c,d));
  // int e = v[i].F+v[i].S
}

//the macros defined can be used in the code and will be changed before compilation....